# Copy to .env for local dev. Vite exposes only vars prefixed with VITE_.

# Base URL of the backend API (apps/api). Requests use credentials: "include"
# so the httpOnly auth cookies are sent automatically.
VITE_API_URL="http://localhost:4000/api"
